# F09 YOLO Box — 60 images

Deterministic nested subset of `datasets/f09_box` using seed `2569`.

- Images: train 42, val 9, test 9
- Objects: train 296, val 63, test 63
- Classes: `lane`, `track-line`, `sideway`
- Smaller datasets are contained in larger datasets within each split.
